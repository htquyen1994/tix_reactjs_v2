import { createMuiTheme } from '@material-ui/core';

const themeMain = createMuiTheme({
    palette: {
        primary: {
            light: "#4a4a4a",
            main: "#4a4a4a",
            dark: "#4a4a4a",
        },
        secondary: {
            light: "#fb4226",
            main: "#fb4226",
            dark: "#fb4226",
        }
    },
});

export default themeMain;